{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WalletGuardPolicy",
  "description": "Policy schema for AGENTROPOLIS-PAYRAIL wallet guardrails. No agent gets raw wallet power.",
  "type": "object",
  "required": [
    "policyId",
    "agentId",
    "maxSpendPerTaskMinorUnits",
    "maxSpendPerDayMinorUnits",
    "approvalThresholdMinorUnits",
    "allowedDistricts",
    "blockedDistricts",
    "dryRun"
  ],
  "additionalProperties": false,
  "properties": {
    "policyId": {
      "type": "string",
      "description": "Unique identifier for this policy"
    },
    "agentId": {
      "type": "string",
      "description": "The agent this policy governs. Use '*' for a global default."
    },
    "allowedDistricts": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Districts this agent is permitted to bill. Use ['*'] to allow all districts not explicitly blocked."
    },
    "blockedDistricts": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Districts this agent is NEVER permitted to bill, regardless of other rules."
    },
    "dryRun": {
      "type": "boolean",
      "description": "When true, all payment flows are simulated — no real funds move. Always true in development."
    },
    "notes": {
      "type": "string",
      "description": "Human-readable notes about this policy."
    },
    "maxSpendPerTaskMinorUnits": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)$",
      "description": "Maximum USDC minor units allowed per individual task."
    },
    "maxSpendPerDayMinorUnits": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)$",
      "description": "Maximum USDC minor units allowed per day."
    },
    "approvalThresholdMinorUnits": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)$",
      "description": "Amounts at or above this USDC minor-unit threshold require approval."
    }
  }
}
