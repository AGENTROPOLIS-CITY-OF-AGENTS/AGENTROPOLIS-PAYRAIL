{
  "name": "@agentropolis/x402-adapter",
  "version": "0.2.0",
  "description": "Guarded x402 / USDC settlement adapters for AGENTROPOLIS-PAYRAIL",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run build && node --test test/*.test.cjs",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentropolis/payrail-core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
